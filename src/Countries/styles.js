import { makeStyles } from "@material-ui/core";
export default makeStyles({
    container: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        margin:'0 auto'
    },
});